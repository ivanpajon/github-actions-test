on:
  pull_request_target:
    types:
      - closed

permissions:
  pull-requests: read
  contents: read

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged